# MultiChat - Локальный запуск и шаринг

## 📋 Первичная настройка (один раз)

### 1. Установите зависимости Backend
```bash
cd backend
pip install -r requirements.txt
cd ..
```

### 2. Установите зависимости Frontend
```bash
cd frontend
npm install
cd ..
```

### 3. (Опционально) Установите CloudFlared для шаринга
Для создания публичных ссылок: https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/install-and-setup/installation/

---

## 🚀 Запуск приложения

### Вариант 1: Через меню
```bash
START.bat
```
Выберите:
- `1` - Запустить оба сервера (рекомендуется)
- `2` - Только Backend (порт 8008)
- `3` - Только Frontend (порт 3000)

### Вариант 2: Напрямую
```bash
start-all.bat
```
Запускает оба сервера в отдельных окнах

### Вариант 3: По отдельности
```bash
start-backend.bat   # Backend на порту 8008
start-frontend.bat  # Frontend на порту 3000
```

---

## 🌐 Шаринг с коллегами через CloudPub

### Шаг 1: Запустите оба сервера
```bash
start-all.bat
```

### Шаг 2: Создайте публичный туннель
В отдельном терминале выполните:
```bash
cloudflared tunnel --url http://localhost:3000
```

### Шаг 3: Поделитесь ссылкой
CloudFlared покажет публичную ссылку типа:
```
https://spirally-soaring-skimmer.cloudpub.ru/
```

**✅ Одна ссылка даёт доступ ко всему приложению!**

---

## ⚙️ Как это работает

```
CloudPub ссылка
      ↓
http://localhost:3000 (Vite Frontend)
      ↓ (автоматический proxy)
  /api/* запросы
      ↓
http://localhost:8008 (FastAPI Backend)
```

### Почему работает через одну ссылку:
1. **Вы открываете туннель только на порт 3000** (Frontend)
2. **Vite автоматически проксирует** все запросы `/api/*` на Backend (порт 8000)
3. **Коллеги получают доступ** и к интерфейсу, и к API через одну ссылку

### Настройки прокси (уже готово):
- **Frontend**: `vite.config.js` 
  - прокси `/api/*` → `http://localhost:8008`
  - `allowedHosts: ['all']` - разрешены все домены (включая CloudPub)
  - `host: true` - слушает на всех интерфейсах
- **Backend**: CORS разрешён для всех доменов (включая `*.cloudpub.ru`)

---

## 🛑 Остановка сервисов

Закройте окна терминалов или нажмите `Ctrl+C` в каждом окне.

---

## 📝 Полезные команды

### Проверка работы
```bash
# Backend API docs
http://localhost:8008/docs

# Frontend
http://localhost:3000

# Health check
http://localhost:8008/health
```

### Логи
Логи отображаются в окнах терминалов каждого сервиса.

---

## ⚡ Быстрая справка

| Команда | Что делает |
|---------|------------|
| `START.bat` | Интерактивное меню выбора |
| `start-all.bat` | Запуск Backend + Frontend |
| `start-backend.bat` | Только Backend (порт 8008) |
| `start-frontend.bat` | Только Frontend (порт 3000) |

**Для шаринга**: После запуска `start-all.bat` → запустите `cloudflared tunnel --url http://localhost:3000`
